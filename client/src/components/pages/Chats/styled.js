import styled from 'styled-components';
import { size } from 'polished';

export const ChatsStyled = styled.div`
  && {
    ${size('100%')};
    display: flex;
    align-items: stretch;
  }
`;

export default null;
